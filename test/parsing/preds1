types
    loc
    value

preds
    pointsto : loc * value -> iProp
    pointsto : loc * value -> iProp
    pointsto : loc * value -> iProp
    pointsto : loc * value -> iProp
    pointsto : loc * value -> iProp

init
